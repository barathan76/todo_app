import 'package:flutter/material.dart';

Widget dismissableContainer({required Color color}) {
  return Container(
    decoration: BoxDecoration(
      borderRadius: BorderRadius.circular(10),
      color: color,
    ),
  );
}
